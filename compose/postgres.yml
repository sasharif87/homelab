services:
  postgres:
    image: postgres:16-alpine
    init: true
    container_name: postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - TZ=America/Chicago
    volumes:
      - /mnt/apps/appdata/postgres:/var/lib/postgresql/data
